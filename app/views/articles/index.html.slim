h1 記事一覧

.news-container
  - @articles.each do |article|
    .news-card
      - if article.category.nil?
        = link_to article_path(article.slug), class: "news-link" do
          .news-title = article.title
      - else
        = link_to category_article_path(slug: article.slug, category_name: article.category.name), class: "news-link" do
          .news-title = article.title

= paginate @articles